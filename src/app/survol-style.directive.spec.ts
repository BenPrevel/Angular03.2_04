import { SurvolStyleDirective } from './survol-style.directive';

describe('SurvolStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new SurvolStyleDirective();
    expect(directive).toBeTruthy();
  });
});
